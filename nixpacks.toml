[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["echo 'Dependencies will be installed during build'"]

[phases.build]
cmds = ["bash build.sh"]

[start]
cmd = "cd backend && node src/index.js"
